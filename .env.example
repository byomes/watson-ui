# Generate hash: python3 -c "import bcrypt; print(bcrypt.hashpw(b'yourpassword', bcrypt.gensalt()).decode())"
WATSON_PASSWORD_HASH=

# Anthropic API key (used until Beelink Watson API is live)
ANTHROPIC_API_KEY=

# Watson Beelink API (set when Beelink is ready)
# WATSON_API_URL=http://watson.local:8000
